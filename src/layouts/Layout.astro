---
/*
 * Shared document layout for every Tailspin Toys page.
 *
 * Defines page metadata, global styles, shared landmarks, and the early
 * high-contrast preference restore script used before page content renders.
 */
import Header from "../components/Header.astro";
import "../styles/global.css";

interface Props {
  /** Page title rendered in the document title element. */
  title?: string;
}

const { title = "Tailspin Toys" } = Astro.props;
---

<!doctype html>
<html lang="en" class="dark">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title}</title>
        <!-- Preconnect to improve font loading -->
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet" />
        <script is:inline>
            (() => {
                const preferenceKey = 'tailspin-high-contrast';

                try {
                    document.documentElement.classList.toggle(
                        'high-contrast',
                        localStorage.getItem(preferenceKey) === 'true',
                    );
                } catch (error) {
                    console.warn('Unable to restore high-contrast preference.', error);
                }
            })();
        </script>
    </head>
    <body class="min-h-screen bg-slate-900 text-white transition-colors duration-300 m-0 w-full h-full font-sans high-contrast:!bg-black high-contrast:!text-white">
        <Header />
        <main class="container mx-auto py-6 h-full max-w-7xl">
            <div class="px-4 sm:px-6 lg:px-8">
                <slot />
            </div>
        </main>
    </body>
</html>

<style is:global>
    :root {
        --font-sans: 'Inter', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
    }
    
    html {
        font-family: var(--font-sans);
        scroll-behavior: smooth;
    }
    
    body {
        -webkit-font-smoothing: antialiased;
        -moz-osx-font-smoothing: grayscale;
    }
</style>
